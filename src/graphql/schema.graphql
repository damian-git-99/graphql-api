type Query {
  user(id: ID!): User,
  users: [User],
}

type Mutation {
  signIn(name: String!, password: String!, email: String!): User,
  logIn(email: String!, password: String!): String
}

type User {
  id: ID!,
  name: String,
  email: String,
}